# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
*.tfvars
!*.tfvars.example
backend.hcl
crash.log
override.tf
override.tf.json
# NOTE: .terraform.lock.hcl is intentionally NOT ignored — commit it so every
# run (local and CI) resolves identical provider versions and hashes.

# Bicep
*.azrm.json
whatif.txt

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Secrets
*.pem
*.pfx
.env
